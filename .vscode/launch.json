{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run start",
      "name": "Run Docusaurus",
      "request": "launch",
      "type": "node-terminal",
      "resolveSourceMapLocations": ["${workspaceFolder}/**", "!**/node_modules/**"],
      "cwd": "${workspaceFolder}"
    }
  ],
  "compounds": [
    {
      "name": "Docusaurus 🦕",
      "configurations": ["Run Docusaurus"]
    }
  ]
}
