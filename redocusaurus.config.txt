[
  'redocusaurus',
  ({
    debug: Boolean(process.env.DEBUG || process.env.CI),
    specs: [
      {
        id: 'api',
        spec: 'data/cippapi.yaml',
        specUrl: `${process.env.DEPLOY_BASE_URL || '/'}cippapi-spec.yaml`,
        routePath: '/api',
      },
    ],
    theme: {
      primaryColor: 'var(--ifm-color-primary)',
      redocOptions: {
        hideDownloadButton: false,
        disableSearch: true,
      }
    }
  }),
],