# Upstream Sync Checkpoint — 2026-08-03

Cycle type: **Light/medium delta** (upstream **10.7.3 → 10.7.5**).

| Repo | From | To | Upstream tip |
|---|---|---|---|
| CIPP (frontend) | 10.7.3 (`d72c1e21b`) | **10.7.5** | `upstream/main` @ `274880fd0` |
| CIPP-API | 10.7.3 (`f1b2c93d5`) | **10.7.5** | `upstream/master` @ `245d9d35b` |

Branches: `manage365/upstream-sync-cipp-20260803` /
`manage365/upstream-sync-cipp-api-20260803`.
Backup tags: `backup/pre-upstream-sync-cipp-20260803` /
`backup/pre-upstream-sync-cipp-api-20260803`.

Delta docs: [UPSTREAM_DELTA_CIPP_20260803.md](./UPSTREAM_DELTA_CIPP_20260803.md),
CIPP-API `docs/upstream-sync/UPSTREAM_DELTA_CIPP_API_20260803.md`.

## Applied

| Batch | Theme | Outcome |
|---|---|---|
| 1 | AzBobbyTables 3.6.2 + `Add`/`Get`/`Remove-CIPPAzDataTableEntity` + call-site renames + ConversionTable | **Apply** |
| 2 | Intune template drift helpers (`Get-CIPPIntunePolicyName`, `Merge-CIPPIntuneTemplateIdentity`, `Select-CIPPIntuneAvailableSetting`), standard/compare updates, SafeLinks, Get-CIPPDrift Microsoft-managed skip | **Apply** (31/31 Intune helper Pester) |
| 3 | `disableNesting` (New-CIPPGroup + FE form), EditGroup `AddDevice` + MEM device action + adapted groups edit dialog, SAM `RoleManagement.Read.Exchange` Scope | **Adapt** (preserved fork groupType/mailNickname + redesigned edit page) |
| 4–5 | Snooze Reason (FE+API), default Home test suite, partner webhook stale-URL + `Get-CIPPHostname`, M365Licenses.json, audit log V2 + webhook tests | **Apply / Adapt** (21/21 `Test-CIPPAuditLogRules` Pester) |

## Skipped / Deferred

| Item | Outcome | Why |
|---|---|---|
| SSO preconsent / `CippSSOSettings` / `Invoke-ExecSSOSetup` | **Skip** | SSO family — SWA/Craft (2026-07-15 decision) |
| `CippAppServiceDomains` / `ExecAppServiceDomains` | **Defer** | No App Service custom-domain hosting in fork |
| `Start-SchedulerOrchestrator` `$ValidTypes` destructive cleanup | **Still defer** | Carryover from 07-28 |
| Teams V2, MCP, Worker health, container management, Purview DLP | Unchanged backlog | See PROCESS.md Feature intake backlog |

## Verification

- Frontend: `npm run build` — pass
- Pester: Intune helpers 31/31; `Test-CIPPAuditLogRules` 21/21
- AST parse: all newly ported `.ps1` files OK
- SAMManifest JSON valid after Scope union-merge

## Versions

- Manage365 **5.30.0 → 5.31.0**; upstream baseline **10.7.3 → 10.7.5**
- API `version_latest.txt` and `Config/version_latest.txt` both **10.7.5**

## Post-deploy actions

1. Deploy Static Web App
2. Deploy **all** Function App slots (AzBobbyTables + CIPPCore changes)
3. **CPV refresh** — new Graph delegated Scope `RoleManagement.Read.Exchange` (`3bc15058-…`)
4. Smoke: Intune template drift, MEM device Add-to-Group, group create with disableNesting, partner webhook page, snooze with reason

## Next cycle base SHAs

| Repo | Base |
|---|---|
| CIPP | `274880fd0` (upstream/main) |
| CIPP-API | `245d9d35b` (upstream/master) |
