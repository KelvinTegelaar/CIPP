# Upstream Sync Checkpoint — 2026-07-27

Cycle type: **Major delta**. Baseline moved:

| Repo | From | To | Upstream ref |
|---|---|---|---|
| CIPP (frontend) | 10.6.1 (`17ed2b396`) | **10.7.2** | `upstream/main` @ `44ac45831` |
| CIPP-API | 10.6.1 (tag) | **10.7.2** | `upstream/master` @ `d63b7fae3` |

Branches: `manage365/upstream-sync-cipp-20260727` / `manage365/upstream-sync-cipp-api-20260727`.
Backup tags: `backup/pre-upstream-sync-cipp-20260727` / `backup/pre-upstream-sync-cipp-api-20260727`.
Triage detail: `UPSTREAM_DELTA_CIPP_20260727.md` (this repo) and `UPSTREAM_DELTA_CIPP_API_20260727.md` (API repo).

## Structural note

Upstream moved to the **CyberDrain org**; releases 10.7.0–10.7.2 landed as
single-parent squash commits. Cycle was executed as: (1) cherry-pick true
commits individually, (2) port squash-only content per accepted feature area
via path-scoped diffs against the upstream tip.

## Applied

- **CIPP-API**: 54 commits on the sync branch (cherry-picks + squash ports).
- **CIPP**: 29 commits on the sync branch (cherry-picks + squash port + icon fix).

### Highlights (both repos)

- **SharePoint permissions report family** — new permissions-report page, PDF
  report primitives (`CippPdf/`), `PermissionsReportButton`/`SharingReportButton`,
  `CippLibraryPermissionsDialog` (Manage Permissions), `CippCheckUserAccessDialog`
  (Check User Access) wired into the fork's `useCippSiteActions` hook.
- **SharePoint templated deployments v1 + hardening** — template builder,
  deploy drawer with live job progress (new opt-in `jobProgress` prop grafted
  into the fork's protected `CippApiResults`), `CippMultiQueueTracker`,
  `CippQueryRefreshButton`, template schemas, nav entries.
- **Sharing report risk analytics** — anonymous-edit/never-expiring links,
  folder shares, external recipients (page taken wholesale; verified verbatim
  upstream-derived, no fork customization).
- **Policy compare** — new `CippPolicyDiffTable`/`CippPolicyCompareDialog`;
  compare-policies page refactored onto the shared table, fork branding and
  single-result fix preserved.
- **401 flash fix** — adapted surgically into the fork's fast-path
  `PrivateRoute` (loading guard) + `authmecipp` persistence exclusion in `_app.js`.
- **Custom test save/run UX** — `useCippFormPageActions` context added to the
  fork's `CippFormPage` (fork submit tooltip/cleaned-values pipeline kept).
- **Intune** — fuzzy policy matching (`Find-CIPPFuzzyPolicyMatch` +
  levenshteinDistance), app template deploy standard, MAM endpoint fixes,
  @odata.type preservation, append-default assignments.
- **Standards** — SmartLockout (Enforce), DisableInactiveUsers,
  IntuneAppTemplateDeploy, NudgeMFA group targeting/registration campaign,
  DisableM365GroupUsers AllowedGroupName/CreateGroup, ConditionalAccessTemplate
  report/warn enabled, SPOVersionControl validators, Copilot settings/limited
  mode surfaced in FE (API implementations exist from 07-15 intake).
- **Identity** — password writeback for synced-user resets (fork's password
  parameter kept), reset-password helper text, group updates routed by
  effective type.
- **Site (in)activity cache and report**, mailbox quota alert types, shadow AI
  alerting, GDAP roles/age-group updates, CISA/ORCA/ZTNA test fixes.

### Curated/protected merges

- `Config/standards.json` (API) and `src/data/standards.json` (FE): union-merged,
  fork formatting preserved (text-level edits, no reserialization).
- `Config/SAMManifest.json` / `PermissionsTranslator.json`: union merges (PIM +
  CopilotPolicySettings permissions). CPV refresh required after deploy.
- Fork nav (`layouts/config.js`), `CippUserActions`, `CippAddEditUser`,
  mailboxes and teams-activity pages: fork versions kept, upstream changes
  applied surgically.

## Skipped / Deferred (see delta docs for full list)

- Sherweb migration tasks, CSP license page actions (fork excludes Sherweb/CSP).
- Container management (`CippContainerManagement`) — not in fork.
- Teams V2 (`New-TeamsRequestV2` + vendored MicrosoftTeams module removal) — deferred, large blast radius.
- SSO dialogs / forced SSO migration — deferred (SSO family intake).
- `custom-domains.js` + `ExecAppServiceDomains` — deferred.
- Upstream CI workflows (fork uses its own Azure SWA workflow), version bumps,
  dashboard v1.
- `CippAnonymizedReportAlert` on mailboxes/teams-activity pages — partial: alert
  component taken, integration into those two fork-customized pages deferred.

## Cleanup

- Removed 10 tracked `" 2"` Finder-duplicate files in CIPP-API from the 07-15
  Copilot intake (duplicate .ps1 files define functions twice at module load).

## Verification

- `yarn build` (Node 22.13.0): pass — all pages incl. new
  permissions-report / sharepoint-templates / compare-policies routes.
- PowerShell AST parse of all 138 changed API .ps1 files: pass.
- JSON validation Config/* both repos: pass (pre-existing BOM on two
  TransportRuleTemplate files and intentionally-empty blank.json noted, not
  from this cycle).
- ESLint on all hand-merged files: clean.

## Versions

- Manage365 5.27.0 → **5.28.0**; upstream baseline 10.6.1 → **10.7.2**
  (`package.json`, `public/version.json`, `public/manage365-version.json`,
  API `version_latest.txt`).

## Post-deploy actions

1. CPV refresh to push new SAM permissions (PIM RoleManagement, Copilot policy) to client tenants.
2. Smoke test: SP permissions report, SP template deploy (job progress), sharing report sync, login flow (401 flash), reset password on a synced user.
